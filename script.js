document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-demo]').forEach(b=>b.addEventListener('click',e=>{e.preventDefault();alert('Demo only — connect your real checkout or contact method before publishing sales.')}));document.querySelectorAll('form').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();alert('Demo form — connect it to your real email or backend before publishing.')}));});
